# GLM Provider 实施计划

## 📋 概述

参考 [.specs/006-glm.md](../.specs/006-glm.md) 规范，实现智谱 GLM Provider，作为 MiniMax Provider 的平行替代方案。

---

## 🎯 核心目标

1. **实现 GLMProvider**：封装智谱 GLM API，实现 smolagents `Model` 接口
2. **OpenAI SDK 集成**：使用 OpenAI SDK 兼容接口调用 GLM API
3. **接口一致性**：与 MinimaxProvider 保持接口一致，可无缝切换
4. **完整测试**：单元测试和集成测试覆盖核心功能
5. **配置管理**：支持环境变量配置 API Key 和模型参数

---

## 📝 实施步骤

### 阶段 1：环境准备

#### 1.1 检查依赖

- [x] 检查 `zai` 包是否已安装
  ```bash
  pip list | grep zai
  ```
- [x] 确认项目已有 `zai` 依赖，无需额外安装

#### 1.2 验证依赖

- [x] 测试 zai 包导入
  ```python
  python -c "from zai import ZhipuAiClient; print('OK')"
  ```

---

### 阶段 2：实现 GLMProvider 类

#### 2.1 创建文件

- [x] 创建 `packages/windows-app/agent/providers/glm_provider.py`

#### 2.2 实现核心代码

- [x] 实现 `GLMProvider` 类定义
  - 继承 `smolagents.models.Model`
  - 实现 `__init__()` 方法

- [x] 实现 Lazy Client 初始化
  - 添加 `_client` 属性
  - 实现 `client` property
  - 使用 `ZhipuAiClient`

- [x] 实现 `generate()` 方法
  - 接收 `List[ChatMessage]` 参数
  - 转换为 zai API 格式（内联转换，无独立方法）
  - 调用 `client.chat.completions.create()`
  - 提取响应文本
  - 返回 `ChatMessage` 对象

- [x] 实现 `__call__()` 方法
  - 接收 `List[Dict[str, str]]` 参数
  - 转换为 `ChatMessage` 格式
  - 调用 `generate()` 方法
  - 返回文本字符串（strip 处理）

- [x] 实现错误处理
  - 捕获异常
  - 封装为 `RuntimeError` 并提供清晰错误信息

#### 2.3 代码结构参考

```python
"""
GLM Provider for smolagents

实现 smolagents 的 Model 接口，封装智谱 GLM API 调用
使用 zai 包（ZhipuAiClient）
"""

from typing import List, Dict, Any, Optional
from smolagents.models import Model, ChatMessage, MessageRole
from zai import ZhipuAiClient


class GLMProvider(Model):
    """
    智谱 GLM Provider

    实现 smolagents 的 Model 接口，封装智谱 GLM API 调用
    使用 zai 包（ZhipuAiClient）
    """

    def __init__(self, api_key: str, model: str = "glm-4-flash"):
        """初始化 GLM Provider"""
        ...

    @property
    def client(self) -> ZhipuAiClient:
        """Lazy init client"""
        ...

    def generate(
        self,
        messages: List[ChatMessage],
        stop_sequences: Optional[List[str]] = None,
        response_format: Optional[Dict[str, str]] = None,
        tools_to_call_from: Optional[List] = None,
        **kwargs
    ) -> ChatMessage:
        """调用 GLM API，返回 ChatMessage"""
        ...

    def __call__(
        self,
        messages: List[Dict[str, str]],
        temperature: float = 0.7,
        max_tokens: int = 2000,
        **kwargs
    ) -> str:
        """调用 GLM API，返回完整响应文本"""
        ...
```

---

### 阶段 3：更新模块导出

#### 3.1 更新 __init__.py

- [x] 编辑 `packages/windows-app/agent/providers/__init__.py`
- [x] 导入 `GLMProvider`
  ```python
  from .glm_provider import GLMProvider
  ```
- [x] 添加到 `__all__`
  ```python
  __all__ = [
      "MinimaxProvider",
      "GLMProvider",
  ]
  ```

---

### 阶段 4：编写单元测试

#### 4.1 创建测试文件

- [ ] 创建 `packages/windows-app/agent/providers/tests/test_glm_provider.py`

#### 4.2 实现测试用例

- [ ] 测试初始化
  ```python
  def test_glm_provider_init():
      provider = GLMProvider(api_key="test-key")
      assert provider.api_key == "test-key"
      assert provider.model == "glm-4-flash"
  ```

- [ ] 测试自定义模型
  ```python
  def test_glm_provider_custom_model():
      provider = GLMProvider(api_key="test-key", model="glm-4")
      assert provider.model == "glm-4"
  ```

- [ ] 测试消息转换（如果实现了独立方法）
  ```python
  def test_convert_messages():
      provider = GLMProvider(api_key="test-key")
      messages = [
          ChatMessage(role=MessageRole.SYSTEM, content="System prompt"),
          ChatMessage(role=MessageRole.USER, content="User message"),
      ]
      result = provider._convert_messages(messages)
      assert result[0]["role"] == "system"
      assert result[1]["role"] == "user"
  ```

- [ ] 测试 `generate()` 方法（需要 mock API）
  ```python
  @patch('zai.ZhipuAiClient')
  def test_generate(mock_client):
      # Mock API 响应
      ...
  ```

- [ ] 测试 `__call__()` 方法
  ```python
  def test_call_method():
      # 测试字典格式输入
      ...
  ```

- [ ] 测试错误处理
  ```python
  def test_api_error_handling():
      # 测试 API 错误封装
      ...
  ```

#### 4.3 运行测试

- [ ] 运行测试套件
  ```bash
  cd packages/windows-app
  pytest agent/providers/tests/test_glm_provider.py -v
  ```

---

### 阶段 5：集成测试

#### 5.1 配置环境变量

- [ ] 创建或编辑 `.env` 文件
  ```bash
  GLM_API_KEY=your-glm-api-key
  GLM_MODEL=glm-4-flash
  ```

#### 5.2 编写集成测试脚本

- [ ] 创建 `packages/windows-app/agent/providers/tests/test_glm_integration.py`

- [ ] 测试真实 API 调用
  ```python
  import os
  from dotenv import load_dotenv

  def test_glm_real_api():
      load_dotenv()
      api_key = os.getenv("GLM_API_KEY")
      if not api_key:
          pytest.skip("GLM_API_KEY not set")

      provider = GLMProvider(api_key=api_key)
      messages = [{"role": "user", "content": "你好"}]
      response = provider(messages)
      assert isinstance(response, str)
      assert len(response) > 0
  ```

- [ ] 测试与 Wisadel Agent 集成
  ```python
  def test_glm_with_wisadel():
      load_dotenv()
      api_key = os.getenv("GLM_API_KEY")
      if not api_key:
          pytest.skip("GLM_API_KEY not set")

      from agent import Wisadel
      model = GLMProvider(api_key=api_key, model="glm-4-flash")
      agent = Wisadel(model=model)
      result = agent.run("测试文本")
      assert result is not None
  ```

#### 5.3 运行集成测试

- [ ] 运行集成测试
  ```bash
  pytest agent/providers/tests/test_glm_integration.py -v -s
  ```

---

### 阶段 6：手动验证和调试

#### 6.1 交互式测试

- [ ] 在 Python REPL 中测试
  ```python
  from agent.providers import GLMProvider
  from smolagents.models import ChatMessage, MessageRole

  provider = GLMProvider(api_key="your-key", model="glm-4-flash")

  messages = [
      ChatMessage(role=MessageRole.USER, content="你好，请介绍一下自己")
  ]

  response = provider.generate(messages)
  print(response.content)
  ```

#### 6.2 测试与现有 Agent 集成

- [ ] 修改 `main.py` 或 GUI 代码，临时切换到 GLMProvider
  ```python
  from agent.providers import GLMProvider

  model = GLMProvider(
      api_key=os.getenv("GLM_API_KEY"),
      model="glm-4-flash"
  )
  ```

- [ ] 运行应用，测试润色功能是否正常工作

---

### 阶段 7：文档和配置

#### 7.1 更新文档

- [ ] 更新 `README.md`，添加 GLM Provider 使用说明
  ```markdown
  ## 支持的 LLM Provider

  ### 智谱 GLM

  配置环境变量：
  \```bash
  GLM_API_KEY=your-glm-api-key
  GLM_MODEL=glm-4-flash  # 可选
  \```

  使用示例：
  \```python
  from agent.providers import GLMProvider
  model = GLMProvider(api_key=os.getenv("GLM_API_KEY"))
  \```
  ```

#### 7.2 添加配置示例

- [ ] 创建 `.env.example` 或更新现有文件
  ```bash
  # MiniMax Provider
  MINIMAX_API_KEY=your-minimax-api-key
  MINIMAX_MODEL=MiniMax-M2.1

  # GLM Provider
  GLM_API_KEY=your-glm-api-key
  GLM_MODEL=glm-4-flash
  ```

---

### 阶段 8：验证和优化

#### 8.1 运行完整测试套件

- [ ] 运行所有测试
  ```bash
  cd packages/windows-app
  pytest agent/providers/tests/ -v
  ```

- [ ] 确保所有测试通过

#### 8.2 性能测试（可选）

- [ ] 测试响应时间
- [ ] 测试并发请求
- [ ] 对比 MinimaxProvider 性能

#### 8.3 代码审查

- [ ] 检查代码风格是否一致
- [ ] 确保错误处理完善
- [ ] 验证类型注解正确
- [ ] 确保文档字符串完整

---

## ✅ MVP 验证指标

根据 spec，以下指标必须全部通过：

- [x] **指标 1**：`GLMProvider` 成功实现 `Model` 接口
- [x] **指标 2**：`ChatMessage` 和 API 格式转换正确
- [x] **指标 3**：与 `MinimaxProvider` 接口保持一致
- [ ] **指标 4**：能够正常调用智谱 GLM API，返回响应
- [ ] **指标 5**：系统消息（system prompt）正确传递
- [ ] **指标 6**：错误处理完善，异常信息清晰
- [ ] **指标 7**：可在 `Wisadel` Agent 中无缝切换使用
- [ ] **指标 8**：单元测试覆盖核心功能

---

## 🏗️ 项目结构

```
packages/windows-app/
├── agent/
│   ├── providers/
│   │   ├── __init__.py           # 导出 GLMProvider, MinimaxProvider
│   │   ├── minimax_provider.py   # MiniMax Provider（已实现）
│   │   ├── glm_provider.py       # GLM Provider（新增）★
│   │   └── tests/
│   │       ├── __init__.py
│   │       ├── test_glm_provider.py      # 单元测试（新增）★
│   │       └── test_glm_integration.py   # 集成测试（新增）★
│   ├── agents/
│   ├── tools/
│   └── ...
├── .env                          # 环境变量配置
├── .env.example                  # 配置示例（更新）★
└── requirements.txt              # 依赖清单（更新）★
```

**标记说明**：
- ★ = 本次实施新增或修改的文件

---

## 📌 注意事项

### API Key 安全

1. **不要硬编码 API Key**：使用环境变量
2. **不要提交 .env 文件**：确保 `.env` 在 `.gitignore` 中
3. **使用 .env.example**：提供配置模板

### 依赖管理

1. **zai 包版本**：确保项目中已有 zai 包
2. **虚拟环境**：使用项目统一的 venv
3. **依赖冲突**：检查是否与现有依赖冲突

### 错误处理

1. **API 错误**：网络超时、限流、配额等
2. **认证错误**：API Key 无效或过期
3. **响应格式**：处理空响应或异常格式

### 测试覆盖

1. **Mock 测试**：单元测试使用 mock，避免真实 API 调用（mock `zai.ZhipuAiClient`）
2. **集成测试**：需要真实 API Key，可设置为可选
3. **边界情况**：测试空消息、超长文本等

### 代码质量

1. **类型注解**：使用 typing 模块
2. **文档字符串**：所有公共方法都需要 docstring
3. **代码风格**：与现有代码保持一致
4. **命名规范**：遵循 Python PEP 8

---

## 🔄 与 MinimaxProvider 对比验证

完成实施后，应验证两个 Provider 的行为一致性：

### 接口一致性

- [ ] 初始化参数相同（api_key, model）
- [ ] `generate()` 方法签名一致
- [ ] `__call__()` 方法签名一致
- [ ] 返回值类型一致

### 功能一致性

- [ ] 相同输入产生合理输出
- [ ] 系统消息处理方式一致
- [ ] 错误处理模式相似

### 切换测试

- [ ] 在 Wisadel 中切换 Provider，功能正常
  ```python
  # 使用 MinimaxProvider
  model = MinimaxProvider(api_key=...)
  agent = Wisadel(model=model)
  result1 = agent.run("测试文本")

  # 切换到 GLMProvider
  model = GLMProvider(api_key=...)
  agent = Wisadel(model=model)
  result2 = agent.run("测试文本")

  # 验证两者都能正常工作
  assert result1 is not None
  assert result2 is not None
  ```

---

## 📅 预估时间

- 阶段 1：环境准备 - ✅ 已完成
- 阶段 2：实现 GLMProvider - ✅ 已完成
- 阶段 3：更新模块导出 - ✅ 已完成
- 阶段 4：编写单元测试 - 1.5 小时
- 阶段 5：集成测试 - 1 小时
- 阶段 6：手动验证 - 0.5 小时
- 阶段 7：文档和配置 - 0.5 小时
- 阶段 8：验证和优化 - 0.5 小时

**已完成**：约 3 小时
**剩余**：约 4 小时

---

## 🎯 成功标准

实施完成后，应满足以下标准：

1. ✅ 所有单元测试通过
2. ✅ 集成测试成功调用 GLM API
3. ✅ 可在 Wisadel Agent 中无缝切换使用
4. ✅ 错误处理完善，异常信息清晰
5. ✅ 代码风格与现有代码一致
6. ✅ 文档完整，使用示例清晰
7. ✅ 与 MinimaxProvider 接口一致

---

## 🔗 参考资料

- [.specs/006-glm.md](../.specs/006-glm.md) - GLM Provider 规范
- [packages/windows-app/agent/providers/minimax_provider.py](../packages/windows-app/agent/providers/minimax_provider.py) - MiniMax Provider 参考实现
- [packages/windows-app/agent/providers/glm_provider.py](../packages/windows-app/agent/providers/glm_provider.py) - GLM Provider 实现
- [智谱 AI 开放平台文档](https://open.bigmodel.cn/dev/api)
- [zai 包文档](https://github.com/zhipuai/zai)
- [smolagents 文档](https://github.com/huggingface/smolagents)
